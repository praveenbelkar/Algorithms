package com.algorithm.praveen.dynamicprogram;

public class CuttingRod {

    public void cuttingRod(int[] lengths, int[] profit, int length) {
        int[] maxProfitArray = new int[lengths.length+1];
        for(int i=0; i < lengths.length; i++) {
            maxProfitArray[i] = 0;
        }

        for(int i=0; i < lengths.length; i++) {
            //if()
        }
    }
}
